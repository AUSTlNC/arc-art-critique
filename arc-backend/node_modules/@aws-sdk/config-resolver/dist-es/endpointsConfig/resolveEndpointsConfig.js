import { __assign } from "tslib";
import { getEndpointFromRegion } from "./utils/getEndpointFromRegion";
import { normalizeEndpoint } from "./utils/normalizeEndpoint";
export var resolveEndpointsConfig = function (input) {
    var _a;
    return (__assign(__assign({}, input), { tls: (_a = input.tls) !== null && _a !== void 0 ? _a : true, endpoint: input.endpoint
            ? normalizeEndpoint(__assign(__assign({}, input), { endpoint: input.endpoint }))
            : function () { return getEndpointFromRegion(input); }, isCustomEndpoint: input.endpoint ? true : false }));
};
